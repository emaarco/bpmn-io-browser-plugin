# Keep every new dependency pinned to an exact version (no ^ / ~ ranges), so
# installs stay reproducible and match what was reviewed. See docs/PUBLISHING.md
# and the pinned lockfile committed alongside.
save-exact=true
