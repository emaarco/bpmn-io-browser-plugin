<!--
PR title must follow Conventional Commits, e.g. `feat: add github MR diff`.
It is validated automatically.
-->

## What & why

<!-- What does this change and why? Link the issue it closes. -->

Closes #

## How to test

<!-- Steps to verify the change (load unpacked, open a .bpmn blob / MR, run tests). -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run lint` and `npm run typecheck` pass
- [ ] Verified in the browser (Chrome and/or Firefox) if UI changed
