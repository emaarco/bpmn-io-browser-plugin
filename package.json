{
  "name": "bpmn-io-browser-plugin",
  "version": "0.0.0",
  "private": true,
  "description": "Browser extension that renders and diffs BPMN files inline on GitLab & GitHub — plus a standalone .bpmn viewer.",
  "type": "module",
  "license": "MIT",
  "author": "Marco Schäck (https://github.com/emaarco)",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/core",
    "packages/bot",
    "packages/extension"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w @bpmn-io-browser-plugin/extension",
    "dev:example": "npm run dev:example -w @bpmn-io-browser-plugin/extension",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "npm run test:e2e -w @bpmn-io-browser-plugin/extension --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "lint:deps": "npm run lint:deps --workspaces --if-present",
    "knip": "knip",
    "knip:fix": "knip --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.8.0",
    "eslint-config-prettier": "10.1.8",
    "knip": "6.32.0",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.66.0"
  }
}
